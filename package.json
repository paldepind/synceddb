{
  "name": "SyncedDB",
  "version": "0.0.1",
  "description": "IndexedDB wrapper with syncing",
  "main": "synceddb.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "na",
  "devDependencies": {
    "mocha": "^2.0.1"
  }
}
